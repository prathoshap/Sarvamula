{
  "name": "sarvamula-app",
  "version": "1.0.0",
  "private": true,
  "description": "Sarvamūla — offline reader of Madhvācārya's Sarvamūla-grantha",
  "scripts": {
    "sync-web": "rm -rf www && mkdir -p www && cp -R ../web/. www/ && rm -f www/serve.py && rm -rf www/analytics/sarvamula.db",
    "sync": "npm run sync-web && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "sharp": "^0.35.3"
  }
}
